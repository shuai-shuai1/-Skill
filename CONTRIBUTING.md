# 贡献指南

## 原则

贡献必须提升可验证性、可复现性或任务路由清晰度。不要仅增加更长的提示词，不要把某一篇私人论文的规则硬编码成通用规范。

## 开发流程

1. 先用一个可复现的失败案例说明问题。
2. 判断修改应进入 `SKILL.md`、`references/`、`scripts/` 还是模板。
3. 采用最小改动并保持现有 CLI 和 CSV 字段兼容。
4. 添加匿名化或 synthetic 测试夹具。
5. 运行单元测试、技能验证和打包检查。
6. 在提交说明中列出行为变化、验证结果和兼容性影响。

## 内容边界

- 不提交未公开论文、真实个人信息、私有实验路径、密钥或未获授权的图。
- 外部规范只能总结或引用，不能大段复制未知许可证内容。
- 示例数据必须明确写为 synthetic、demo 或 anonymized。
- 任何自动检查都必须说明它是启发式规则还是确定性验证。

## 代码风格

- 默认 Python 3.10+。
- 优先标准库，Pillow 仅用于图像检查。
- 使用 `argparse`、明确退出码和 UTF-8 文件读写。
- 不吞异常，不静默回退，不覆盖现有输出文件，除非显式传入 `--force`。
